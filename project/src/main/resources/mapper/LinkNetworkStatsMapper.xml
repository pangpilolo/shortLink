<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pangpi.shortlink.project.dao.mapper.LinkNetworkStatsMapper">
    <insert id="shortLinkNetworkState">
        INSERT INTO t_link_network_stats (full_short_url, gid, date, cnt, network, create_time, update_time, del_flag)
        VALUES (#{stats.fullShortUrl}, #{stats.gid}, #{stats.date},
                #{stats.cnt}, #{stats.network}, #{stats.createTime}, #{stats.updateTime}, #{stats.delFlag})
        ON DUPLICATE KEY UPDATE cnt = cnt + #{stats.cnt};
    </insert>
    
    <select id="listNetworkStatsByShortLink" resultType="com.pangpi.shortlink.project.dao.entity.LinkNetworkStatsDO">
        SELECT tlns.network,
               SUM(tlns.cnt) AS cnt
        FROM t_link tl
                 INNER JOIN
             t_link_network_stats tlns ON tl.full_short_url = tlns.full_short_url
        WHERE tlns.full_short_url = #{param.fullShortUrl}
          AND tl.gid = #{param.gid}
          AND tl.del_flag = '0'
          AND tl.enable_status = #{param.enableStatus}
          AND tlns.date BETWEEN #{param.startDate} and #{param.endDate}
        GROUP BY tlns.full_short_url, tl.gid, tlns.network;
    </select>
    
    <select id="listNetworkStatsByGroup" resultType="com.pangpi.shortlink.project.dao.entity.LinkNetworkStatsDO">
        SELECT tlns.network,
               SUM(tlns.cnt) AS cnt
        FROM t_link tl
                 INNER JOIN
             t_link_network_stats tlns ON tl.full_short_url = tlns.full_short_url
        WHERE tl.gid = #{param.gid}
          AND tl.del_flag = '0'
          AND tl.enable_status = '0'
          AND tlns.date BETWEEN #{param.startDate} and #{param.endDate}
        GROUP BY tl.gid, tlns.network;
    </select>
</mapper>